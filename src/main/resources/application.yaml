spring:
  application:
    name: advanced-rag

  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
    vectorstore:
      pgvector:
        initialize-schema: false
        dimensions: 1536

  datasource:
    url: jdbc:postgresql://localhost:5432/support_db
    username: postgres
    password: postgres